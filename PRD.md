# PRD: 사무실 공용 물품(소모품) 관리 웹앱

- 문서 버전: v1.0
- 작성일: 2026-07-24
- 작성자: PM (minkyo1112@gmail.com)
- 상태: 초안 (오픈 이슈 확인 필요)

---

## 1. 개요 (배경 / 문제 정의)

사무실에서는 볼펜, A4용지, 건전지, 물티슈 등 공용 소모품을 10~30명의 직원이 함께 사용한다. 현재는 누가 무엇을 얼마나 가져갔는지에 대한 기록이 전혀 남지 않기 때문에, 재고가 실제로 바닥나는 "필요한 순간"이 되어서야 부족한 사실을 알게 되는 문제가 반복되고 있다.

이 웹앱은 물품의 등록·수량 조정·삭제라는 최소한의 조작만으로 재고 현황을 실시간으로 파악하고, 물품별로 설정된 적정 재고량 대비 부족 여부를 미리 확인할 수 있게 하는 것을 목표로 한다. 로그인 시스템 없이 닉네임만으로 누구나 바로 사용할 수 있는 가벼운 사내 도구로 설계한다.

## 2. 목표 및 성공 지표

**목표**
- 물품별 현재 수량과 적정 재고량을 한눈에 확인할 수 있게 한다.
- 수량 변경(입고/출고) 시 "누가, 언제, 무엇을, 얼마나" 변경했는지가 자동으로 기록되게 한다.
- 재고 부족 물품을 미리 인지할 수 있게 한다(부족해지고 나서가 아니라).

**성공 지표 (확인 필요)**
사용자로부터 별도의 목표 수치를 받지 않아 아래는 제안 후보이며, 실측 없이 확정할 수 없다. 담당자 확인 후 확정 필요.
- (제안) 재고 소진으로 인한 "긴급 구매/요청" 건수 도입 전 대비 감소율
- (제안) 대시보드의 "재고 부족" 위젯을 통해 사전에 발견된 부족 물품 비율
- (정성 지표 대안) 도입 후 2~4주 시점에 실제 사용자(총무/관리 담당자) 인터뷰로 "부족 상황을 사전에 알았는가" 여부 확인

**확인 필요 질문**: 목표로 하는 정량 지표(예: 월 몇 건 이상 사용 로그 기록, 재고 부족 사전 발견율 목표치)가 있는지 확인 부탁드립니다.

## 3. 대상 사용자

- 사무실 내 공용 물품을 함께 사용하는 전 직원 (약 10~30명)
- 별도 로그인 계정 없이, 사용 시점에 닉네임만 입력하여 누구나 등록/조정 가능
- 물품 재고를 관리·점검하는 총무/운영 담당자(비정기적으로 대시보드를 확인하는 역할)

## 4. 요구사항

### 4.1 기능 요구사항

| 우선순위 | 요구사항 | 설명 |
|---|---|---|
| P0 | 물품 등록 | 이름/카테고리/수량/적정 재고량/입고일/등록자 닉네임을 입력해 신규 물품 등록 |
| P0 | 물품 목록 조회 | 등록된 전체 물품을 목록으로 확인 |
| P0 | 수량 늘리기/줄이기 | 물품 목록에서 개별 물품의 수량을 증감시키고, 변경 이력을 자동 기록 |
| P0 | 물품 삭제 | 더 이상 관리하지 않는 물품을 목록에서 삭제(삭제 이력은 로그에 남김) |
| P0 | 재고 부족 판정 | 물품별 "현재 수량 < 적정 재고량" 조건으로 부족 여부·부족 수량 계산 |
| P0 | 대시보드 4개 위젯 | 총 품목 수 / 재고 부족 / 카테고리별 분포 도넛 차트 / 최근 변경 내역 |
| P0 | 엑셀 업로드(일괄 등록/업데이트) | 엑셀 파일을 업로드해 물품을 일괄 등록하거나 기존 물품 수량을 업데이트 |
| P1 | 엑셀 업로드 결과 리포트 | 성공/실패 행을 구분해 사용자에게 명확히 안내 |
| P1 | 변경 이력 상세 조회 | 특정 물품 클릭 시 해당 물품의 전체 변경 이력(로그) 조회 |
| P2 | 엑셀 업로드 템플릿 다운로드 | 업로드용 표준 엑셀 양식 제공 |
| P2 | 변경 취소(undo) | 직전 수량 변경을 되돌리는 기능 |

### 4.2 비기능 요구사항

- **성능**: 동시 사용자 10~30명 수준의 소규모 사내 트래픽을 전제로 하며, 별도의 고가용성/스케일링 설계는 불필요.
- **접근성**: 사내 PC/브라우저에서 별도 설치 없이 URL 접속만으로 사용 가능해야 함.
- **보안**: 로그인이 없는 구조이므로 사내망/사내 공유 링크로만 배포하는 것을 전제로 하며, 민감 정보(개인정보, 결제정보 등)는 다루지 않음. (세부 내용은 9장 RLS 노트 참고)
- **데이터 무결성**: 수량은 음수가 될 수 없어야 하며(0 미만 방지), 모든 수량 변경은 예외 없이 로그 테이블에 기록되어야 함.

## 5. 화면 구성 및 화면별 상세

화면은 정확히 아래 4개로 구성한다. 추가 화면은 이번 범위에 포함하지 않는다.

| 파일명 | 화면명 | 역할 |
|---|---|---|
| `index.html` | 대시보드 | 최초 진입 화면. 재고 현황 요약 4개 위젯 표시 |
| `item.html` | 물품 목록 | 물품별 수량 조정(증가/감소)·삭제 액션 수행 |
| `register.html` | 물품 등록 | 신규 물품 1건 등록 |
| `import.html` | 엑셀 업로드 | 엑셀 파일로 물품 일괄 등록/업데이트 |

> **오타 정정 코멘트**: 사용자 요청 원문에는 `register.gtml`로 표기되어 있었으나, 이는 오타이며 실제 파일명은 `register.html`이 맞습니다. 본 문서 및 실제 구현에서는 `register.html`을 사용합니다.

### 5.1 `index.html` — 대시보드

- **목적**: 앱 진입 시 현재 재고 상태를 한 화면에서 요약 파악.
- **주요 UI 요소**:
  - 상단 4개 위젯 카드/영역 (6장 참고): 총 품목 수, 재고 부족, 카테고리별 분포 도넛 차트, 최근 변경 내역
  - 물품 목록/등록/엑셀 업로드 화면으로 이동하는 네비게이션(상단 메뉴 또는 버튼)
- **필요 데이터**: `items` 테이블 전체(집계용), `item_logs` 테이블 최근 N건
- **액션**: 페이지 진입 시 자동 조회(별도 사용자 입력 액션 없음). "재고 부족" 위젯 클릭 시 `item.html`의 부족 물품만 필터된 화면으로 이동(선택 사항, P1).

### 5.2 `item.html` — 물품 목록

- **목적**: 등록된 물품을 확인하고, 개별 물품의 수량을 조정하거나 삭제.
- **주요 UI 요소**:
  - 물품 리스트/테이블: 이름, 카테고리, 현재 수량, 적정 재고량, 부족 여부 배지(부족 시 "부족 -N개" 형태로 강조 표시), 입고일, 등록자 닉네임, 수정 시간
  - 각 행별 액션: 수량 증가 버튼(+, 수량 입력 후 반영), 수량 감소 버튼(-, 수량 입력 후 반영), 삭제 버튼(삭제 확인 다이얼로그)
  - 카테고리 필터(문구류/전자기기/청소용품/기타), 부족 물품만 보기 토글(선택, P1)
  - 수량 증감 시 "닉네임" 입력 필드(누가 변경했는지 기록하기 위함) — 필수 입력
- **필요 데이터/액션**:
  - 조회: `items` 테이블 전체
  - 증가/감소: `items.quantity` 갱신 + `item_logs`에 변경 전/후 수량, 변경량, 액션 종류(INCREASE/DECREASE), 실행자 닉네임, 시각 기록
  - 삭제: `items` 행 삭제 + `item_logs`에 DELETE 액션 기록(스냅샷 이름 보존)

### 5.3 `register.html` — 물품 등록

- **목적**: 신규 공용 물품 1건을 시스템에 등록.
- **주요 UI 요소**: 입력 폼
  - 물품 이름(텍스트, 필수)
  - 카테고리(드롭다운/라디오, 문구류·전자기기·청소용품·기타 4개 중 택1, 필수)
  - 수량(숫자, 필수, 0 이상)
  - 적정 재고량(숫자, 필수, 0 이상)
  - 입고일(날짜 선택, 필수 또는 기본값 오늘)
  - 등록자 닉네임(텍스트, 필수)
  - 등록 버튼 / 취소(목록으로 돌아가기)
- **필요 데이터/액션**: `items`에 신규 행 insert + `item_logs`에 CREATE 액션 기록. 등록 완료 시 `item.html`로 이동하거나 등록 완료 메시지 표시.

### 5.4 `import.html` — 엑셀 업로드

- **목적**: 다수의 물품을 엑셀 파일 하나로 한 번에 등록하거나 기존 물품 수량을 갱신.
- **주요 UI 요소**:
  - 파일 선택(업로드) 버튼, 업로드할 엑셀의 컬럼 형식 안내 텍스트
  - 업로드 후 미리보기 테이블(파싱 결과를 실제 반영 전에 확인)
  - "신규 등록" / "기존 갱신" 행 구분 표시
  - 오류가 있는 행(카테고리 값이 4개 외 값이거나 필수값 누락 등) 강조 표시 및 사유 안내
  - 반영(확정) 버튼, 실행자 닉네임 입력(일괄 작업도 누가 했는지 기록 필요)
- **필요 데이터/액션**: 파싱된 각 행에 대해 물품 이름 기준으로 기존 물품과 매칭 → 있으면 update(주로 수량 갱신), 없으면 insert. 모든 변경 건은 `item_logs`에 IMPORT 액션으로 기록(가능하면 행 단위로 개별 기록해 추적 가능하게 함).

## 6. 재고 부족 판정 로직

- **부족 조건**: `현재 수량(quantity) < 적정 재고량(target_quantity)`
- **부족 수량 계산식**: `부족 수량 = 적정 재고량 - 현재 수량` (조건을 만족할 때만 의미 있는 양수값)
- 이 기준은 물품마다 개별적으로 설정된 `target_quantity`를 기준으로 하므로, 물품별로 부족 여부와 부족 수량이 서로 다르게 계산된다.

**대시보드 "재고 부족" 위젯에서의 표시**
- 상단에 부족 물품 개수(예: "재고 부족 3건")를 숫자로 표시
- 그 아래 부족 물품 목록을 간단히 나열(물품명 + 부족 수량, 예: "A4용지 -20", "건전지 -5"). 목록이 많을 경우 상위 N개만 보여주고 "전체 보기"로 `item.html`의 부족 필터 화면으로 연결(P1).

**물품 목록(`item.html`) 화면에서의 표시**
- 각 물품 행에 부족 여부를 배지/색상으로 표시(예: 부족 시 빨간색 "부족" 배지 + 부족 수량 숫자)
- 부족 물품만 필터링해서 보는 토글 제공(P1, 필수는 아니지만 운영 편의상 권장)

## 7. 데이터 모델 (물품 / Item)

| 필드명 | 컬럼명 | 타입(제안) | 필수 여부 | 설명 |
|---|---|---|---|---|
| 물품 이름 | `name` | text | 필수 | 물품명 |
| 카테고리 | `category` | text (제약: 4개 값 중 하나) | 필수 | 문구류 / 전자기기 / 청소용품 / 기타 — 4개로 고정, 확장 불가 |
| 수량 | `quantity` | integer | 필수 | 현재 재고 수량, 0 이상 |
| 적정 재고량 | `target_quantity` | integer | 필수 | 물품마다 개별 설정하는 목표 재고 수량 |
| 입고일 | `received_date` | date | 필수 또는 기본값(오늘) | 최근 입고/등록 기준일 |
| 등록자 닉네임 | `created_by_nickname` | text | 필수 | 로그인 없이 입력받는 닉네임 |
| 수정 시간 | `updated_at` | timestamptz | 자동 | 최근 수정 시각(트리거로 자동 갱신) |

## 8. 대시보드 위젯 (정확히 4개, 아래 순서 고정)

1. **총 품목 수**: `items` 테이블의 전체 행 개수
2. **재고 부족**: 6장의 부족 판정 로직 기준으로 부족한 물품의 개수 및 목록(물품명 + 부족 수량)
3. **카테고리별 분포 도넛 차트**: 문구류 / 전자기기 / 청소용품 / 기타 4개 카테고리별 물품 수(또는 수량 합) 비중을 도넛 차트로 표시
4. **최근 변경 내역**: `item_logs`에서 최근 N건(예: 10건)을 시간 역순으로 표시. 각 항목에 등록자/실행자 닉네임, 변경 시각, 변경 내용(어떤 물품을 얼마나 늘렸는지/줄였는지/등록/삭제했는지)을 표기

> 위 4개 외의 위젯은 이번 범위에 추가하지 않는다.

## 9. Supabase 스키마 제안

### 9.1 `items` (물품 마스터)

```sql
create table public.items (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  category text not null check (category in ('문구류', '전자기기', '청소용품', '기타')),
  quantity integer not null default 0 check (quantity >= 0),
  target_quantity integer not null default 0 check (target_quantity >= 0),
  received_date date not null default current_date,
  created_by_nickname text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- updated_at 자동 갱신 트리거
create or replace function public.set_updated_at()
returns trigger as $$
begin
  new.updated_at = now();
  return new;
end;
$$ language plpgsql;

create trigger trg_items_updated_at
before update on public.items
for each row execute function public.set_updated_at();
```

### 9.2 `item_logs` (수량 변경 이력 / 감사 추적)

"누가 무엇을 얼마나 가져갔는지" 문제를 해결하는 핵심 테이블. 물품이 삭제되어도 이력은 남아야 하므로 이름 스냅샷을 별도로 저장한다.

```sql
create table public.item_logs (
  id uuid primary key default gen_random_uuid(),
  item_id uuid references public.items(id) on delete set null,
  item_name_snapshot text not null,
  action text not null check (action in ('CREATE', 'INCREASE', 'DECREASE', 'DELETE', 'IMPORT')),
  quantity_before integer,
  quantity_after integer,
  quantity_change integer,
  actor_nickname text not null,
  memo text,
  created_at timestamptz not null default now()
);

create index idx_item_logs_created_at on public.item_logs (created_at desc);
create index idx_item_logs_item_id on public.item_logs (item_id);
```

- `대시보드 최근 변경 내역` 위젯: `select * from item_logs order by created_at desc limit 10`
- `물품별 이력 조회`(P1): `select * from item_logs where item_id = :id order by created_at desc`

### 9.3 RLS(Row Level Security) 방침

로그인 시스템이 없는 사내 도구이므로, 최소한의 운영을 위해 아래처럼 단순하게 구성한다.

```sql
alter table public.items enable row level security;
alter table public.item_logs enable row level security;

create policy "anon_full_access_items" on public.items
for all
to anon
using (true)
with check (true);

create policy "anon_full_access_item_logs" on public.item_logs
for all
to anon
using (true)
with check (true);
```

> **주의(Note)**: 위 정책은 `anon` 키로 누구나 전체 read/write가 가능한 매우 개방적인 설정이다. 사내망/사내 링크 공유 등 제한된 접근 환경에서만 사용하는 것을 전제로 하며, 외부에 URL이 노출되는 순간 누구나 데이터를 읽고 조작할 수 있다. 실무 배포 시에는 최소한 (1) 링크를 사내 VPN/내부망으로 제한하거나, (2) 간단한 접근 비밀번호(앱 레벨) 또는 (3) Supabase Edge Function을 경유한 서버측 검증 도입을 검토할 것을 권장한다.

## 10. 기술 스택 / 아키텍처 제안

**기본 방향(권장, 소규모 사내 도구 기준)**
- 정적 HTML 4개(`index.html`, `item.html`, `register.html`, `import.html`) + 순수 JS(또는 최소한의 유틸)로 구성
- Supabase JS 클라이언트를 CDN `<script>` 태그로 로드하여 별도 빌드 도구 없이 바로 사용
- 차트는 Chart.js를 CDN으로 로드해 도넛 차트 구현
- 정적 파일이므로 사내 파일 서버, GitHub Pages, Netlify/Vercel 등 어디든 손쉽게 배포 가능

**트레이드오프(대안 검토)**
- React/Vue + Vite 같은 프레임워크: 화면 간 상태 공유(예: 부족 필터 유지)나 컴포넌트 재사용에는 유리하지만, 화면 4개짜리 소규모 도구 대비 빌드 파이프라인·의존성 관리 비용이 과함. 향후 화면/기능이 크게 늘어날 경우에만 전환을 고려.
- 서버(Node/Express 등) 도입: 엑셀 파싱이나 검증 로직을 서버에서 처리하면 보안/검증 측면에서 유리하지만, 10~30명 규모의 사내 도구에서는 배포·운영 부담이 커짐. 우선은 클라이언트에서 파싱 후 Supabase에 직접 upsert하는 방식으로 충분.
- 위 이유로 **1안(정적 HTML 4개 + Supabase JS 클라이언트)**을 기본 채택안으로 제안.

**Supabase 연동 방식**
- 각 HTML에서 Supabase JS 클라이언트를 초기화(`createClient(url, anonKey)`)
- `index.html`: 페이지 로드 시 `items`, `item_logs` 조회 후 4개 위젯 렌더링
- `item.html`: `items` 목록 조회 + 행별 수량 변경/삭제 시 Supabase update/delete 호출과 함께 `item_logs` insert를 함께 수행(하나의 트랜잭션처럼 처리하되, Supabase REST 특성상 두 단계 호출이 되므로 실패 시 사용자에게 재시도 안내)
- `register.html`: 폼 제출 시 `items` insert + `item_logs`(CREATE) insert
- `import.html`: 클라이언트에서 파싱 → 검증 → 물품명 매칭 후 upsert(또는 개별 insert/update) → 각 행에 대해 `item_logs`(IMPORT) insert

**엑셀 업로드(`import.html`) 처리 방식 상세**
- **파싱 라이브러리**: SheetJS(xlsx) — CDN으로 로드해 브라우저에서 `.xlsx`/`.csv` 파일을 클라이언트 사이드에서 파싱(서버 불필요)
- **컬럼 매핑(제안, 헤더명 기준)**:
  | 엑셀 헤더 | 매핑 필드 |
  |---|---|
  | 물품이름 | `name` |
  | 카테고리 | `category` |
  | 수량 | `quantity` |
  | 적정재고량 | `target_quantity` |
  | 입고일 | `received_date` |
  | 등록자닉네임 | `created_by_nickname` |
- **신규/기존 판단 기준**: 업로드 시점의 `items.name`(물품 이름, 대소문자/공백 트림 후 완전 일치)으로 매칭. 일치하는 기존 물품이 있으면 "갱신"(주로 수량·적정재고량 업데이트), 없으면 "신규 등록"으로 분류. (물품명이 유일 식별자 역할을 하므로, 동일 이름의 물품이 중복 등록되지 않도록 안내 문구 필요)
- **에러 처리**: 카테고리 값이 4개 고정값 외의 값이면 오류 행으로 분류하고 반영하지 않음. 필수 필드(이름/카테고리/수량/적정재고량/등록자 닉네임) 누락 시에도 오류 행으로 분류. 미리보기 화면에서 정상 행과 오류 행을 구분 표시하고, 사용자가 오류 행을 인지한 상태에서 "정상 행만 반영" 버튼으로 확정하도록 함. 반영 결과(성공/실패 건수)는 완료 후 요약 메시지로 안내(상세 리포트는 P1).

## 11. 범위 제외 (이번에는 하지 않는 것)

- 로그인/계정/권한 관리(관리자·일반 사용자 구분 없음) — 닉네임 입력만으로 운영
- 다중 사업장/다중 창고/다중 지점 재고 관리
- 재고 부족 시 이메일/슬랙/문자 등 자동 알림 발송
- 발주/구매 자동화(부족 물품을 자동으로 구매 요청하는 기능)
- 카테고리 추가/수정/삭제(4개 고정, 커스터마이즈 불가)
- 모바일 전용 앱(반응형 웹으로 충분, 별도 네이티브 앱 없음)
- 다국어 지원
- 변경 이력에 대한 승인 워크플로우(누구나 즉시 반영, 사후 승인 없음)

## 12. 일정 / 마일스톤

사용자로부터 구체적인 출시 목표일을 받지 못해 날짜는 확정하지 않았다(13장 오픈 이슈 참고). 아래는 작업 간 의존 관계를 기준으로 한 단계 구분이며, 목표일 확정 시 역산해 채워 넣을 것을 권장한다.

| 단계 | 내용 | 선행 조건 |
|---|---|---|
| M1. 데이터 기반 확정 | Supabase 프로젝트 생성, `items`/`item_logs` 테이블·RLS 정책 적용 | 없음(최우선 착수 가능) |
| M2. 핵심 CRUD 개발 | `register.html`(등록), `item.html`(목록/수량조정/삭제) 개발 | M1 완료 |
| M3. 대시보드 개발 | `index.html` 4개 위젯(총 품목 수/재고 부족/카테고리 도넛/최근 변경 내역) | M1, M2의 로그 기록 로직 완료 |
| M4. 엑셀 업로드 개발 | `import.html`(파싱/검증/미리보기/반영) | M1, M2 완료(동일한 insert/update 로직 재사용) |
| M5. 통합 테스트 및 배포 | 4개 화면 통합 점검, 사내 배포(URL 공유) | M1~M4 모두 완료 |

**확인 필요**: 목표 출시일(또는 사내 공유 예정일)이 있다면 알려주시면 위 단계별 날짜를 역산해 채워 넣겠습니다.

## 13. 리스크 및 오픈 이슈

| 항목 | 내용 | 확인 필요 대상 |
|---|---|---|
| 성공 지표 미확정 | 정량 목표 수치가 없어 2장의 지표는 제안 수준에 머무름 | 사용자(요청자) 확인 필요 |
| 출시 목표일 미확정 | 마감일이 없어 12장 일정은 상대적 순서만 제시 | 사용자(요청자) 확인 필요 |
| 물품 식별자 이슈 | 엑셀 업로드/재고 조정 시 "물품 이름"을 유일 식별자로 사용하는데, 동명이인 물품(예: 같은 이름, 다른 규격)이 생기면 오매칭 위험 있음 | 이름 중복을 어떻게 막을지(예: UNIQUE 제약 여부) 논의 필요 |
| RLS 개방 정책의 보안 리스크 | anon 키로 전체 read/write를 허용하므로 URL 유출 시 데이터 훼손 가능 | 사내망 제한/간단 비밀번호 도입 여부 결정 필요 |
| 동시 수정 충돌 | 여러 사람이 동시에 같은 물품 수량을 조정할 경우 마지막 쓰기가 이전 변경을 덮어쓸 수 있음(현재 설계는 낙관적 갱신 전제) | 트래픽이 실제로 문제될지 운영 중 모니터링, 필요 시 DB 함수(원자적 증감)로 보완 |
| 엑셀 업로드 컬럼 헤더 고정 여부 | 10장에서 제안한 헤더명이 실제 사용자들이 쓰는 엑셀 양식과 다를 수 있음 | 실제 사용 중인 엑셀 양식이 있다면 공유 요청 |

---

## 변경 이력

| 날짜 | 변경 내용 | 사유 |
|---|---|---|
| 2026-07-24 | 최초 작성 (v1.0) | 신규 PRD 작성 요청 |
